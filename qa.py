def for_elaine():
    oldf = None
    try:
        oldf = io.open('/root/eline/for_elaine.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def with_nanae():
    oldf = None
    try:
        oldf = io.open('/root/eline/with_nanae.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def with_friend():
    oldf = None
    try:
        oldf = io.open('/root/eline/with_friend.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_happy():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_happy.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_tired():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_tired.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_sad():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_sad.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_under1year():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_under1year.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_1years():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_1years', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_3years():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_3years.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_work():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_work.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_relax():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_relax.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_weekend():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_weekend.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_brother():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_brother.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_parents():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_parents.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_kid():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_kid.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_family():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_family.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()            

def random_film_year_2020s():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_year_2020s', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_year_2015s():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_year_2015s', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_year_2010s():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_year_2010s.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_year_2000s():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_year_2000s', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_language_Chinese():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_language_Chinese.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_language_English():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_language_English.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_language_Small():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_language_Small.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_year_Japanese():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_year_Japanese.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_story():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_story.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()


def random_film_suspense():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_suspense.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()


def random_film_love():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_love.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_action():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_action.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_crime():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_crime.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_comedy():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_comedy.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_scary():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_scary.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

def random_film_science():
    oldf = None
    try:
        oldf = io.open('/root/eline/random_film_science.csv', 'r',encoding='UTF-8')
        lines = oldf.readlines()
        total_num=len(lines)
        ret_idx = random.sample(range(0,total_num), 1)[0]
        return str(lines[ret_idx]).replace(',', '\n')
    finally:
        if oldf:
            oldf.close()

Q_A = {
    "我可以如何帮到你~": {
        "option": [
            "1. 推荐【某类型】电影",
            "2. 【个性化】推荐电影",
            "3. 【电影名】寻找资源",

        ],
        "answer": {
            "1": "跟谁一起看电影",
            "2": "根据什么标准筛选？",
            "3": "告诉我电影名字吧？",
            "4": "Elaine跟谁看呀？",
        }
    },
    "Elaine跟谁看呀？": {
        "option": [
            "1. 自己看",
            "2. 跟Nanae看",
            "3. 跟朋友看",
        ],
        "answer": {
            "1": for_elaine,
            "2": with_nanae,
            "3": with_friend,
        }
    },
    "跟谁一起看电影？": {
        "option": [
            "1. 自己一个人看",
            "2. 跟对象一起看",
            "3. 跟朋友一起看",
            "4. 跟家人一起看",
        ],
        "answer": {
            "1": "描述一下你的状态？",
            "2": "在一起多久了？",
            "3": "现在是什么时间点？",
            "4": "有没有小朋友或长辈？",
        }
    },
    "描述一下你的状态？": {
        "option": [
            "1. 精力充沛",
            "2. 略微疲倦",
            "3. 有些压抑",
        ],
        "answer": {
            "1": random_film_happy,
            "2": random_film_tired,
            "3": random_film_sad,
        }
    },
    "在一起多久了？": {
        "option": [
            "1. 1年内",
            "2. 1到3年",
            "3. 3年以上",
        ],
        "answer": {
            "1": random_film_under1year,
            "2": random_film_1years,
            "3": random_film_3years,
        }
    },
    "现在是什么时间点？": {
        "option": [
            "1. 工作中的间隙",
            "2. 工作日下班后",
            "3. 愉快的周末",
        ],
        "answer": {
            "1": random_film_work,
            "2": random_film_relax,
            "3": random_film_weekend,
        }
    },
    "有没有小朋友或长辈？": {
        "option": [
            "1. 都没有",
            "2. 有长辈",
            "3. 有小朋友",
            "4. 都有",
        ],
        "answer": {
            "1": random_film_brother,
            "2": random_film_parents,
            "3": random_film_kid,
            "4": random_film_family,
        }
    },
    "根据什么标准筛选？": {
        "option": [
            "1. 上映「年份」",
            "2. 所用「语言」",
            "3. 具体「类型」",
        ],
        "answer": {
            "1": "你希望的电影年份是？",
            "2": "你希望的电影语言是？",
            "3": "你希望的电影类型是？",
        }
    },
    "你希望的电影年份是？": {
        "option": [
            "1. 半年内新片",
            "2. 最近两年的电影",
            "3. 2010年后的电影",
            "4. 2000年后的电影",
        ],
        "answer": {
            "1": random_film_year_2020s,
            "2": random_film_year_2015s,
            "3": random_film_year_2010s,
            "4": random_film_year_2000s,
        }
    },
    "你希望的电影语言是？": {
        "option": [
            "1. 华语电影",
            "2. 英语电影",
            "3. 小语种电影",
            "4. 日语/韩语电影",
        ],
        "answer": {
            "1": random_film_language_Chinese,
            "2": random_film_language_English,
            "3": random_film_language_Small,
            "4": random_film_year_Japanese,
        }
    },
    "你希望的电影类型是？": {
        "option": [
            "1. 剧情",
            "2. 悬疑",
            "3. 爱情",
            "4. 动作",
            "5. 犯罪",
            "6. 喜剧",
            "7. 恐怖",
            "8. 科幻",
        ],
        "answer": {
            "1": random_film_story,
            "2": random_film_suspense,
            "3": random_film_love,
            "4": random_film_action,
            "5": random_film_crime,
            "6": random_film_comedy,
            "7": random_film_scary,
            "8": random_film_science,
        }
    },
}